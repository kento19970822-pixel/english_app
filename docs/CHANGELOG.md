# 変更履歴 (CHANGELOG)

## [VER-20260830-02] - 2026-08-30
### 修正・改善内容 (初期Ch.1含むキャラ黒シルエット＆解放永続化 ＆ 星座暗記フラグ参照 ＆ リセット機能明確化 ＆ 全単語語義数整合)
- **🔓 初期Ch.1を含むキャラクター解放ライフサイクル (F-13/F-15)**:
  - 初回起動時および「完全リセット」時は、**Ch.1 も含めて全キャラクターが黒シルエット（`locked`）および `? ? ? ? ?`** で表示。
  - 学習を開始（暗記フラグ/暗記ポイント獲得）するとキャラクターが解放（基本形/元気/進化）され、名前も公開。
  - 一度解放されたキャラクターは、忘却曲線による日跨ぎ減算や手動未暗記戻しで 0% になっても **黒シルエットには戻らず基本形（`lowHealth`）を永続維持**。
  - 単語帳下のキャラバナー（`WordChapterBanner`）、図鑑（`CharacterGalleryScreen`）、タイトル（`TitleScreen`）の全UIで完全連動。
- **🌌 暗記単語数の累積可視化 夜空と星と星座コレクション (F-25)**:
  - 参照データを「80ptマスター」から **「暗記済みフラグ（`isMemorized == true`）」** に変更。忘却曲線の自動減算によって完成した星座が勝手に消えたり壊れたりせず、恒久的に輝き続ける安心の達成感を提供。
- **🧹 2つのリセット機能のUI/UX集約と明確化 (F-06/F-09)**:
  - **単語帳メニュー**: 「全単語の暗記フラグを一括クリア」（単語帳の暗記チェックのみを解除する安全な操作）。
  - **記録画面**: 「全学習データ・進捗の完全リセット（初期化）」（全チャプターロック・全キャラ黒シルエット復帰・スタンプ初期化）を一元管理。
- **📖 全単語データ（34,653件）の語義カウント（`total_senses` / `sense_index`）完全整合 (F-01)**:
  - `flat (1/2, 平らな)` ＆ `flat (2/2, アパート)` ＆ `flats (1/1, フラットシューズ)` をはじめ、全多義語のインデックスと総語義数の不整合を100%解消。

## [VER-20260830-01] - 2026-08-30
### 修正・改善内容 (胸バッジトーン・オン・トーン ＆ 起動画面刷新 ＆ 同義語排除 ＆ 全単語精査 ＆ 解放フラグ永続化)
- **🎖️ お気に入りスタンプ胸バッジ 動的トーン・オン・トーン ＆ 微細パールハイライト (F-14)**:
  - スタンプ本体色（`main`）に連動して、彩度・明度を深めたディープトーン同系色外枠 ＋ 1px パールホワイト光沢を動的生成。金縁の一律適用を解消し、全16色で美しく調和。
- **🏷️ 起動画面の完全中央揃え ＆ サブタイトル刷新 (F-18)**:
  - `FittedBox` とレスポンシブな制約により、ネイティブアプリでも枠内に完全に収まりキラキラ絵文字（✨）のはみ出しをゼロ化。
  - サブタイトルを **『脳に刻む、爽快スピード暗記。』** / *Smart Recall Fall Game* に刷新。
- **🎯 出題ロジック セマンティック同義語重複排除 (F-02)**:
  - 「like」出題時の「好き」「好んでいる」や、大小（big/large）、発言（speak/talk/say）等の同義・類義語クラスタおよび漢字語幹重複を検出・相互排除。
- **📖 全単語データ徹底精査 ＆ 「flat」品詞・例文補正 (F-01)**:
  - 「flat」の形容詞（「平らな」）追加、名詞「アパート」補完、複数形「フラットシューズ（flats）」例文修正（*She is wearing flats.*）。対・複数形名詞や不自然なプレースホルダー例文を一括精査・補正。
- **🔓 キャラクター＆チャプター解放フラグの永続化 (F-13)**:
  - チャプター解放済み（`isUnlocked == true`）のキャラクターは、忘却曲線の減衰や手動未暗記戻しで0%になっても `locked`（黒シルエット）に戻らず、基本形（`lowHealth`）を永続維持。
- **🌌 暗記単語数の累積可視化 星座コレクションマップ仕様統合 (F-23)**:
  - 単語暗記（80ptマスター）の積み上がりを夜空の星と星座の完成で可視化する仕様を定義。

## [VER-20260829-05] - 2026-08-29
### 修正・改善内容 (単語帳チャプタードロップダウン8行分・画面下寄り表示)
- **🪟 単語帳チャプタードロップダウンの高さ8行分（384px）表示 (F-06)**:
  - チャプター選択ドロップダウンのメニュー高さを **8行分（`menuMaxHeight: 384.0`）** に設定。
  - 画面中央よりやや下の位置にゆったりと展開され、複数チャプターの一覧性と、上部広大エリアのタップによる即時中断・キャンセルのしやすさを完璧に両立。

## [VER-20260829-04] - 2026-08-29
### 修正・改善内容 (単語帳最上部からの次セクションジャンプ即時遷移 ＆ 全ソート完全追従)
- **⏬ 単語帳最上部（Section 0 表示中）からの「次へ」ジャンプ動作の最適化 (F-06)**:
  - 画面最上部（検索バー・ソートバー・Section 0 表示中）で ⏬ FAB ボタンを押した際、Section 0 が吸着するだけにならず、直ちに **Section 1（例: Chapter 2 / B / 次のカテゴリ）** のヘッダー吸着位置へジャンプ。
  - ソートモード（`chap`, `az`, `category`）およびフィルター適用時の `SliverAppBar` 高さ変動（128px / 155px）に動的追従し、累積ズレ 0.0px で正確に吸着。
- **🏷️ カテゴリソート時のヘッダーアイコン表示修正**:
  - `_sortMode == 'category'` 時にも正しいカテゴリ用アイコン（`Icons.category_rounded`）が確実に表示されるように改善。

## [VER-20260829-03] - 2026-08-29
### 修正・改善内容 (相棒胸バッジ配色1:1完全同期 ＆ 単語帳チャプター指定UI最適化)
- **🎖️ 相棒胸バッジ 16配色パレット 1:1 完全一致同期 (F-14)**:
  - `_getStampMainColor` を `PixelStampWidget` の 16配色パレットと 100% 同期。緑系スタンプ（フォレスト・ハーブ、エメラルド等）を選択した際にバッジが赤くなる不整合を完全解消。
- **🪟 単語帳チャプター指定UI・キャンセル操作性の最適化 (F-06)**:
  - チャプター選択ドロップダウンに `menuMaxHeight: 220.0` を設定し、コンパクトな約4行に制限。画面の65%以上の背景領域を露出し、どこをタップしても即座に中断・キャンセル可能。
  - チャプター指定コンテナに「全章に戻す」クイッククリアボタンを新設。
  - ボトムシートヘッダーに明示的な「×」閉じるボタンを新設し、片手親指で迷いなく脱出可能に。

## [VER-20260829-02] - 2026-08-29
### 修正・改善内容 (単語詳細 Floating音声ボタン ＆ セクションスクロール累積ズレ0px完全解決)
- **🔊 単語詳細 Floating 音声ボタン常時表示 (F-22)**:
  - コンテンツエリアの右下（`Positioned(bottom: 16, right: 16)`）に 52x52px の Floating 音声ボタンを常時浮遊表示。スクロール中も親指でいつでも即座に発音再生可能。
  - ボトムナビバーは `[前へ]` `[閉じる]` `[次へ]` の 3ボタン均等バランス配置に最適化。
- **📐 単語帳セクション前後ジャンプのズレ完全解決 (F-06)**:
  - `WordChapterBanner` を `SizedBox(height: 78.0)` で固定拘束し、内部レイアウトを2行構成（Ch.X + 80pt定着 / プログレスバー + %）に整理してオーバーフローを完全排除。
  - `_calculateSectionOffsets()` と `_getCurrentSectionIndex()` による高精度オフセット計算により、累積ズレ 0.0px を達成。

## [VER-20260829-01] - 2026-08-29
### 修正・改善内容 (単語帳・UI/UX機能改修 ＆ 4大専門エージェント総合レビュー完全反映)
- **🔊 単語詳細 音声ボタン右下移動 (F-22)**:
  - ヘッダー左上からボトムナビ右端の立体エメラルド調ボタン（44x44px）へ移動。親指片手操作性を向上し、TTS安全破棄および語義取得レースコンディション防止を完備。
- **🎖️ 相棒 胸バッジ全面カラー反映 ＆ 二重枠線 (F-14)**:
  - 装備中スタンプのメインカラーをバッジ円全体に反映し、「外枠: 濃茶 + 内枠: 金/白」の二重枠線で全キャラ体色と同化しない高視認性ドット絵描画を実現。
- **📐 チャプター切り替え（次/前）のスクロールズレ解消 (F-06)**:
  - バナー実寸（78.0px）と Prefix Sums アルゴリズムにより、チャプター進行（Ch.10, Ch.30...）に伴う累積誤差を 0px に完全排除。
- **🪟 チャプターフィルターの画面外ディスミス領域確保 (F-06)**:
  - ボトムシート高さを `maxHeight: 0.71` に最適化し、上部に約29%の画面外タップ領域を確保。
- **📅 学習カレンダースタンプの拡大（75%）＆ 重なりレイアウト (F-10/F-15)**:
  - セルを `Stack` 構造化。暗記数（`+N`）表示時もスタンプが縮小せずセル中央に約75%で堂々と大きく表示されるように改善。
- **🏷️ 単語帳チャプターバー vs キャラバナーの参照データ明示化 (F-06/F-13)**:
  - チャプターバー（Sticky Header）: `暗記済: 〇/〇 語 (〇%)`（手動暗記フラグ）
  - キャラクターバナー: `80pt定着: 〇/〇 語 (〇%)`（実質80ptマスター）
  - スワイプ暗記時の `_chapterGlobalStats` O(1) 即時同期。

## [VER-20260828-20] - 2026-08-28
### 修正・最適化内容 (モード切替時スクロール復元・単語帳暗記による新章解放の即時追従)
- **🎮 プレイモード切替時のチャプター表示位置復元 (F-03/F-15)**:
  - 弱点克服・チャレンジから学習モードに戻した際に、選択中の最新チャプターが常に最上部に表示されるようスクロール位置を自動適用。
- **🚀 単語帳暗記による次章解放時の即時最新章追従 (F-03/F-15)**:
  - 単語帳で単語を暗記して新章（例: Ch.4）が解放された際、ゲーム選択画面に戻った時に即座に新章を選択し最上部へスクロール。

## [VER-20260828-19] - 2026-08-28
### 修正・改善内容 (チャプター上部見切れ解消・初回起動時最新チャプター自動選択)
- **🎮 チャプター選択タイルの上部見切れ完全解消 (F-03/F-15)**:
  - リストスクロールオフセットを実寸スロット高（50.0px）に厳密補正し、最上部に4pxパディングを設定。外枠が見切れることなく美しく表示されるように修正。
- **🚀 アプリ初回起動時の最新解放チャプター自動選択 (F-03/F-15)**:
  - 起動直後の初期画面で確実に最新解放チャプター（例: Ch.3）が選択され、最上部にスクロールされるように初期化ライフサイクルを修正。

## [VER-20260828-18] - 2026-08-28
### 画面レイアウト刷新・初期表示仕様確定 (ゲーム選択画面の上部固定・チャプター独立スクロール)
- **🎮 ゲーム選択画面の構造刷新 (F-03/F-15)**:
  - 上部ヘッダー（タイトル/アイコン/プレイモード/難易度レベル）および下部（相棒バナー/開始ボタン）を完全固定化し、見切れをゼロに。
  - 中央のチャプター一覧を独立スクロール領域化し、画面全体をスクロールさせずにチャプター枠内のみで最新解放チャプター（例: Ch.3）が最上部に表示されるように最適化。

## [VER-20260828-17] - 2026-08-28
### 改善・追加内容 (単語帳検索のEnterキー確定化・最新解放チャプター自動選択＆最上部スクロール)
- **🔍 単語帳検索のEnterキー確定化（入力負荷ゼロ・爆速化） (F-06)**:
  - 1文字ごとのリアルタイム検索を撤廃し、Enterキー（または虫眼鏡アイコンタップ）で検索を実行する仕様に変更。35,000件のフィルタリング負荷を無くし軽快な入力を実現。
- **🎮 ゲーム選択画面 最新解放チャプター自動選択＆最上部スクロール (F-03/F-15)**:
  - 各レベル選択時、そのレベル内で解放されている最大チャプターを自動選択し、チャプター一覧の最上部に自動スクロール。

## [VER-20260828-16] - 2026-08-28
### 修正・確定内容 (ゲーム選択画面チャプターリスト達成率を70pt基準へ厳格修正)
- **🎮 ゲーム選択画面チャプターリストの達成率を70pt基準へ更新 (F-03/F-15)**:
  - チャプター解放条件（`70pt以上 90%で次章解放`）と完全連動させ、チャプターリストの各章達成率を **実質70pt以上単語割合（`w.retentionPoint >= 70`）** で算出・保存するように修正。
  - 単語帳のキャラクターバナー（80pt以上基準）、セクションバー（暗記フラグ基準）との仕様分離を確立。

## [VER-20260828-15] - 2026-08-28
### 仕様確定・設計厳格化 (単語帳セクションバーの暗記フラグ＆フィルター反映 vs キャラクターバナーの80pt＆フィルター非依存)
- **🏷️ セクションバー（Chap/A-Z/Cat.）の暗記率仕様 (F-06)**:
  - 暗記フラグ（`w.isMemorized`）を参照し、フィルター条件を反映した表示中の単語での達成率を表示。
- **👑 キャラクターバナーの進捗仕様 (F-06/F-15)**:
  - 実質80pt以上単語率（`w.retentionPoint >= 80`）を参照し、フィルターの影響を受けずにチャプター全体の真の暗記進捗・キャラクター状態を表示。

## [VER-20260828-14] - 2026-08-28
### 修正・統一内容 (単語帳チャプターバナー暗記率を80pt基準へ完全修正・画面間整合性確保)
- **👑 単語帳チャプターバナーの暗記率を80pt基準へ厳格統一 (F-06/F-15)**:
  - `words_screen.dart` の `_chapterGlobalStats` および `WordChapterBanner` 内に残存していた `w.retentionPoint >= 70` を完全撤廃し、**実質80pt以上（`w.retentionPoint >= 80`）** に修正。
  - ゲーム選択画面の達成率（88%）と単語帳バナーの達成率（88%）の不一致を完全解消。

## [VER-20260828-13] - 2026-08-28
### 修正・改善内容 (フラッシュカードフリーズ解消・アニメーション速度チューニング・二段階触覚ハプティクス)
- **🛠️ ドラッグフリーズの完全解消 (F-24)**:
  - ドラッグ進行度通知を `ValueNotifier` による背後カードの局所リビルドに切り替え、親ウィジェットの多重リビルドによるジェスチャー競合・フリーズを完全解消。
- **✨ 飛翔アニメーション速度の自然化 (F-24)**:
  - 画面解像度依存の過剰な超高速移動を排し、固定距離 `600px` / `300ms`（`Curves.easeOutCubic`）による自然で気持ちいい飛翔演出に調整。
- **📳 二段階触覚ハプティクス（スマホ操作性向上） (F-24)**:
  - しきい値到達時に `HapticFeedback.selectionClick()`、スワイプ確定時に `HapticFeedback.mediumImpact()` のリアルな手応えを指先に伝達。

## [VER-20260828-12] - 2026-08-28
### 追加・改善内容 (フラッシュカード モダンUX・目線誘導・フリック・3Dスタック・コンボ・キーボード・要復習リトライ)
- **👁️ 目線誘導（Visual Eye Flow）とストレスフリーレイアウト (F-24)**:
  - 上部（現在地・極細グラデーション進捗バー）➔ 上部直下（直前答え合わせHUD＆コンボバッジ）➔ 中央（黄金比カード・視線集中エリア）➔ 下部（親指集中操作バー）の完全最適化。
- **⚡ 楽観的UI更新（Optimistic UI）＆ TTSキュー完全制御 (F-24)**:
  - スワイプと同時にゼロ遅延で次カードへ遷移し、DB書き込みはバックグラウンド非同期実行（120Hz完全追従）。カード遷移時に前音声を即座に停止し、キュー詰まり・重複再生を完全防止。
- **👆 フリック初速（Velocity）判定 ＆ 動的3Dスタックせり上がり (F-24)**:
  - `650px/s` のフリック初速検知により、親指で軽く弾くだけで爽快にスワイプ。ドラッグ進行度に応じて背後カードが手前に拡大・フェードインするリアルな触感。
- **🔥 連続暗記コンボ演出 ＆ ハプティクスフィードバック (F-24)**:
  - 3連続以上でコンボバッジがポップし、5/10/20連続時に特別な振動演出。
- **⌨️ キーボードショートカット対応 (F-24)**:
  - `←`（要復習）、`→`（暗記完了）、`Space/Enter`（反転フリップ）、`Ctrl+Z/Cmd+Z`（Undo）、`Esc`（終了）のフルキーボード操作に対応。
- **🔄 セッション終了時の「要復習単語だけ再特訓」ボタン (F-24)**:
  - 全カード終了時に、間違えた単語だけをその場で即座にリトライできる黄金SRSループを確立。

## [VER-20260828-11] - 2026-08-28
### 改善・修正内容 (単語帳スワイプ特訓ボタンの2段目配置＆和訳ONボタン高さ30px完全一致)
- **🎴 スワイプ特訓ボタンの配置統一＆高さ30px同期 (F-06/F-24)**:
  - 単語帳の2段目（ソートバー）の「和訳: ON」の並びに「スワイプ特訓」ボタン（高さ30px・角丸8px）を配置。
  - 全単語表示時・フィルター適用時を問わず、常に同じ位置から表示中単語のスワイプ特訓を開始可能に。
  - 3段目サマリー行から重複ボタンを撤廃し、件数・アクティブフィルタータグの視認性を向上。

## [VER-20260828-10] - 2026-08-28
### 修正・改善内容 (フラッシュカードモード矢印テキスト整理・フリップ時音声再生・四角アイコン撤廃)
- **🔊 和 ➔ 英 モードでの裏面（回答）確認時音声自動再生 (F-24)**:
  - 出題時はネタバレ防止のため音声を流さず、カードをタップして裏面（英語）を表示した瞬間に英語音声を自動再生。
- **✨ モード選択テキストの矢印整理 (F-24)**:
  - 0枚目カードおよび下部操作ボタンの外側矢印を削除し、`和 ➔ 英` / `英 ➔ 和` のクリーンな表記に統一。
- **🧹 単語カード右上の四角（反転インジケータ）アイコン撤廃 (F-24)**:
  - 直感的でなくノイズとなっていた右上の反転アイコンを削除し、CEFRバッジのみのシンプルなヘッダーへ整理。

## [VER-20260828-09] - 2026-08-28
### 修正・改善内容 (フラッシュカードUI刷新・片手操作最適化・0枚目スワイプ開始・横長長方形カード化)
- **🎴 フラッシュカード 片手操作下部集中レイアウト (F-24)**:
  - 画面左下に `[ ✕ 終了 ]`、中央に `[ 👈 要復習 / 和➔英 ]` ＆ `[ 暗記完了 / 英➔和 👉 ]`、右下に `[ ↩ 戻す ]` を配置。スマートフォンでの片手親指操作性を大幅向上。
- **🎴 0枚目「モード選択カード」スワイプによる開始 (F-24)**:
  - 開始時に0枚目カードを表示。👉右スワイプで「英➔和」、👈左スワイプで「和➔英」モードを開始。
- **🔊 和訳モード（和➔英）時の自動音声OFF (F-24)**:
  - 和訳モード時は日本語を見て英語を思い出すため、出題時の自動音声読み上げをOFF（ネタバレ防止）。裏面確認時やスピーカータップ時のみ再生。
- **📐 スマホ画面での横長長方形カード（オーバーフロー完全解消） (F-24)**:
  - 黄金比アスペクト比 1.48:1 とレスポンシブ最大サイズ制約（440x250px）により、縦長スマートフォンでも美しい横長長方形で収まり、下部オーバーフローを完全に解消。
- **✨ 上部説明文の削除 ＆ ミニマルHUD (F-24)**:
  - 開始時の「👈 左: 要復習 | 👉 右: 暗記完了」を削除し、直前スワイプの答え合わせHUDのみをスマートに表示。

## [VER-20260828-08] - 2026-08-28
### 追加・修正内容 (暗記特化スワイプフラッシュカードモード新設)
- **🎴 暗記特化スワイプフラッシュカードモード (F-24)**:
  - **没入型ミニマルUI**: 単語暗記・仕分けに100%集中できるフラッシュカード画面（`FlashcardScreen`）。
  - **直感スワイプ**: 👉 右スワイプで暗記済み（`80pt化` / `is_memorized = true`）、👈 左スワイプで要復習（`0pt化` / `is_memorized = false` / `is_restricted = true`）。
  - **Tinderライクなカードスタック**: 次のカードが背後に重なり、傾きながら画面外へ飛ぶアニメーション ＆ `MEMORIZED ✓` / `REVIEW 🔄` スタンプ演出。
  - **3Dカード反転（フリップ）**: タップ時に 3D 回転アニメーションで裏面（和訳 / 英単語・例文・品詞）を表示。
  - **音声（TTS）自動再生**: 新カード表示時に英語音声を自動再生（スピーカータップで再再生）。
  - **直前カード答え合わせ HUD ＆ ↩️ Undo（1手戻す）**: 直前のスワイプ結果を上部に常時表示し、誤スワイプ時は DB 状態（ポイント・フラグ）を含めて即座に復元。
  - **2つの出題モード**: `英 ➔ 和` モード ⇄ `和 ➔ 英` モードを画面上部トグルでワンタップ切替。
  - **起動導線**: 単語帳の各セクションヘッダー（Ch.1等）右端の「特訓」ボタン、および絞り込みフィルターバーからの全絞り込み単語一括起動に対応。
  - **セッション完了リザルト**: 全カード終了時に暗記達成数・要復習数のサマリーポップアップを表示。

## [VER-20260828-07] - 2026-08-28
### 追加・修正内容 (キャラ状態実80pt率連動・チャプター解放70pt率厳格分離 & 単語帳前章移動FAB)
- **👑 キャラクター状態・進化条件の実80pt率連動 (F-13/F-14)**:
  - キャラクターの成長・進化状態（`PixelCharacterWidget.stateFromRate`）は、暗記フラグではなく「現時点の実質 `retention_point >= 80` の単語割合」を直接参照するように厳格化。
  - 忘却曲線による日跨ぎ減算で80pt未満に落ちると元気がなくなり、復習して80pt以上に引き上げると元気・進化する（80%以上で進化）という生き生きとした相棒連動を保証。
- **🔓 チャプター解放基準の厳格分離 (F-03/F-15)**:
  - チャプター解放判定（`checkAndUnlockNextChapter` / `calculateChapterUnlockRate`）は「現時点の実質 `retention_point >= 70` の単語割合が 90% 以上」で判定し、キャラ進化基準と完全に独立したロジックとして分離。
- **⬆️ 単語帳「前チャプターへ移動」FAB ボタン化 (F-06/F-15)**:
  - 単語帳の最上部へ移動FABを「前チャプター/セクションへ移動」機能へ変更。
  - 次チャプターボタンと対になる上向きダブル矢印（`Icons.keyboard_double_arrow_up_rounded` / エメラルドグリーン `#5F9E98` / 白色アイコン）へデザインを統一。
  - 単語帳タブタップによる最上部スクロール＆フィルターリセット機能はそのまま維持。

## [VER-20260828-06] - 2026-08-28
### 追加・修正内容 (ゲームデータ更新タイミング最適化 & 単語カードエッジ色調整)
- **🎮 ゲームデータ更新タイミングの最適化 (F-05/F-15)**:
  - プレイ中のDB即時書き込みをインメモリ蓄積バッファ（`_pendingQuizResults`）へ移行。
  - タイムアップ演出を経てゲームが正常終了したタイミングでのみ、単一トランザクション（`batchUpdateQuizResults`）でDBへ一括コミット。
  - ポーズ画面からの「中止」、リスタート（「最初からやり直す」）、プレイ中のタスクキル・アプリ終了時はバッファを破棄し、不要・不完全なデータがDBに残らない堅牢性を確立。
- **👉 単語カードのスワイプ・アフォーダンス色の更新 (F-06)**:
  - 単語カードの左端にエメラルドグリーン（右スワイプで暗記化）、右端にオレンジ（左スワイプでリセット）のマイクロバーを配置し、操作方向と直感的に一致するUIアフォーダンスへ調整。

## [VER-20260828-05] - 2026-08-28
### 追加・修正内容 (総合改善ロードマップ・フェーズ3: モダンデザイン & マイクロインタラクション)
- **🌟 BouncyScaleTap マイクロインタラクション (F-15/F-16)**:
  - タップ押し込み時のスケール沈み込み（`pressedScale: 0.96`）と、指を離した瞬間のバウンス復帰（`Curves.easeOutBack`）、触覚フィードバック（`HapticFeedback.lightImpact`）を統合した `BouncyScaleTap` を新設・導入。
  - ゲーム選択画面（モードカード・難易度タブ・チャプターカード・開始ボタン・クイックアクション列）および単語帳FABに適用し、リッチで心地よい操作感を実現。
- **👉 単語カードのスワイプ・アフォーダンス（マイクロエッジインジケータ） (F-06)**:
  - 単語カードの左右端にスワイプ操作が可能であることをさりげなく示唆する淡いマイクロ角丸バー（右: 暗記化エメラルド、左: リセットオレンジ）を配置。初見ユーザーでも説明不要で左右スワイプ操作を直感的に認知可能に。

## [VER-20260828-04] - 2026-08-28
### 追加・修正内容 (総合改善ロードマップ・フェーズ2: UI/UX ミニマル化 & 直感性向上)
- **✨ ゲーム選択画面の文字スリム化 & タイポグラフィ整理 (F-15)**:
  - チャプター選択ヘッダー横のバッジを `70pt以上 90%で次章` のコンパクトなミニマルアイコン付きバッジに整理。
  - プレイモードカードのサブタイトルを「章別集中/次章解放」「苦手特訓/反復学習」「1分/100問/ランダム」のスマートキーワードに引き締め、視覚的なファーストビューをモダンに刷新。
- **🔘 難易度レベル選択の視覚的アフォーダンス強化（単一 vs 複数選択） (F-15)**:
  - 学習モード（単一選択）と弱点/チャレンジ（複数選択）の操作仕様を視覚化。複数選択モード時は各レベルボタン右上にチェックマークバッジを表示し、見出し横に「複数選択可」バッジを点灯。
- **🏆 チャプターカードのステータス視覚化 (F-15)**:
  - クリア達成（`✨ 達成 〇%`）、学習中（リアルタイム進捗率）、未解放（🔒 アイコン＋淡いグレー背景）の3状態をひと目で識別可能に。

## [VER-20260828-03] - 2026-08-28
### 追加・修正内容 (総合改善ロードマップ・フェーズ1: パフォーマンス & 堅牢性 最適化)
- **⚡ 全チャプター進捗同期の N+1 クエリ解消 (F-15)**:
  - 374チャプターの個別SELECTループを単一の `GROUP BY chapter` 集計SQLにリファクタリング。全チャプター進捗同期の処理時間を数十〜数百msから **1ms未満** に劇的短縮。
- **🚀 単語帳タブのキャッシュ＆ダーティフラグ管理（完全ノーウェイト表示） (F-06)**:
  - `AppDatabase.wordsDataVersion` を導入。単語帳タブ切り替え時の不要な34,650件全件再ロードを撤廃し、データ変更時のみ差分再読み込みを行うスマートキャッシュ制御を実装。
- **🛡️ 画面遷移・主要アクションボタンの二重タップ防止ガード (F-15/F-16)**:
  - ゲーム選択画面の「学習開始」ボタン、リザルト画面の「もう一度/選択画面へ」ボタン、記録画面の図鑑/カレンダー遷移ボタンに排他ガード（`_isNavigating` / `_isActionProcessing`）を適用し、連打による多重画面遷移・スタック破壊を完全防止。
- **🔒 複数テーブル更新処理のトランザクション保護 (F-15)**:
  - チャプター解放、進捗同期、一括リセット等の複数テーブル更新処理を `transaction` ブロックで保護し、急なバックグラウンド終了時にもデータ不整合が起きない堅牢性を確立。

## [VER-20260828-02] - 2026-08-28
### 追加・修正内容
- **📊 単語帳チャプターバナーのフィルター非依存進捗・キャラクター表示修正 (F-13)**:
  - 単語帳で「未暗記のみ」などのフィルターを適用した際も、チャプターバナーの暗記率およびキャラクター成長状態（シルエット／元気／MASTER進化形態）が 0% にならず、該当チャプター全体の全単語マスター進捗（70pt以上）を参照して正確に表示されるよう修正。
- **🔓 次章（チャプター）解放条件の正式統一（70pt以上が90%以上） (F-15)**:
  - 「該当章における獲得ポイントが70点以上の単語数」が、その章の総単語数の 90%以上 を占めることでクリア判定および次チャプター解放されるよう判定ロジックおよびUI表示テキストを 70pt に完全整合化。
- **📱 単語帳：次セクションへの片手ジャンプFABボタン新設 & 最上部吸着オフセット調整 (F-06)**:
  - スマホを片手（親指）で操作しやすい画面右下に「⏬（アイコンのみ・コンパクト表示）」ボタンを配置。
  - タップ時に `SliverAppBar` および前セクションの高さをミリ秒単位で厳密計算し、対象チャプターのヘッダーバーが画面の最上部にピタッと切り替わって吸着表示されるようスクロールオフセットを完全最適化。
- **🎨 iOSステータスバー表示色（Dark Content）自動制御 (F-10)**:
  - `AppTheme.lightTheme` に `appBarTheme: SystemUiOverlayStyle.dark` を定義し、白背景画面で時計・バッテリー残量等のステータスバー文字が自動的に黒（Dark Content）になるよう制御を修正。
- **⚡ ゲーム選択画面のレベル切替時の非同期競合防止（リクエスト世代ID管理 / F-15)**:
  - 難易度レベル（初級・中級・上級）の連打時に、古い非同期レスポンスを世代番号（`_chapterLoadRequestId`）で破棄し、常に最後に選択されたレベルのチャプター一覧が正確に反映される安全な排他制御を実装。

## [VER-20260828-01] - 2026-08-28
### 追加・修正内容
- **🎴 単語カード3段構成 & 固定高（120px: `itemExtent: 120.0`）対応 (F-06)**:
  - カード内レイアウトを3段構成（1段目: バッジ＋操作アイコン、2段目: 垂直中央配置の英単語、3段目: 日本語の意味/タップで和訳）に変更。
  - `SliverFixedExtentList(itemExtent: 120.0)` を適用し、iPhone 12 mini（幅 375pt）等の小画面でも枠内見切れゼロと O(1) 高速スクロール性能を両立。
- **👆 詳細モーダル開閉ロジックの修正 (F-06)**:
  - キーボード表示判定を `MediaQuery.viewInsets` / `_searchFocusNode` に最適化し、通常時のシングルタップで確実に詳細モーダルが開くように修正。
- **🔘 単語カード内操作ボタンのタップ領域・サイズ拡大 (F-06)**:
  - 音声ボタン（18px）とお気に入り星ボタン（19px）のタップターゲットを `minWidth: 32, minHeight: 32` に拡大。
- **📄 単語詳細モーダルの高さ統一（画面高 82%） & 下スワイプ終了ブラックアウト解消 (F-22)**:
  - モーダル高さを画面の 82% に固定統一し、単語切り替え時の枠ブレを解消。
  - 重複した自前ドラッグ pop を撤廃し、Flutter 標準の安全な Dismiss アニメーションに一本化してブラックアウト不具合を解消。
- **👆 和訳オン時の詳細表示統一 & キーボード表示時のモーダル遮断 (F-06)**:
  - 和訳表示 ON 状態ではシングルタップで詳細モーダルを開く挙動に統一。
  - 検索キーボード表示中はタップ時のグレー反転をオフにし、画面タップ時は詳細モーダルを開かずにキーボードの収納（`unfocus`）を最優先。
- **🔊 音声（TTS）の左右イヤホン同期 & iOS Siri（声4）最優先スコアリング (F-20)**:
  - Bluetooth イヤホン装着時のステレオ同期モード（`allowBluetooth`, `allowBluetoothA2DP`）を適用し、多重再生バッファリングを防止して左右ディレイを解消。
  - ボイス選出スコアリングにおいて iOS Siri（声4 / Voice 4）を最優先（+250pt）で自動適用。
- **📝 単語「fall」の動詞（落ちる・転ぶ）および名詞（秋・落下）の2語義対応 (F-01)**:
  - 「fall」データを語義1: `verb`（落ちる・転ぶ）と語義2: `noun`（秋・落下）の2レコード構成に正確にアップデート。
- **🔍 フィルター内容の全ソート順共通化 (F-06)**:
  - ソートモードによるフィルター非表示を撤廃し、「学習ステータス」「CEFR」「チャプター」「A-Z」「カテゴリ」の全項目を常時共通表示。

## [VER-20260827-10] - 2026-08-27
### 追加・修正内容
- **🎫 ひらめき復習チケット（SRS分散学習 ＆ アクティブ想起ダイアログ / F-23 新設）**:
  - ゲーム選択画面右上のクイックアクション列に4つ目のチケットボタン（🎫）を新設。
  - 復習期日（Due）単語の通知バッジ点灯、SM-2アルゴリズム（4段階自己評価）による忘却曲線間隔最適化。
  - 未学習単語の完全除外（プレイ経験・学習履歴がある単語のみ出題）。
- **🧠 スマート・インターリービング学習順 ＆ 語義番号昇順整列 (F-19 / F-06)**:
  - CEFR難易度最優先（A1〜C2）、頭文字（A〜Z）分散循環配置による干渉効果防止。
  - 同一単語の複数語義における語義番号昇順制約（1 ➔ 2 ➔ 3...）および約100語間隔の分散配置。
- **👆 単語帳ヘッダーの指追従なめらか出現 ＆ 操作時スクロール位置リセット (F-06)**:
  - `SliverAppBar(floating: true, snap: false)` によるリニアなスクロール追従。
  - 検索入力・クリア、ソート切り替え、フィルター付け外し時の自動最上部（offset: 0）スクロール。
- **📊 単語帳チャプター進捗割合（%）表示 ＆ 暗記基準統一 (F-13)**:
  - 各チャプターバナーへの進捗割合（`〇%`）表示と、アプリ全体の暗記基準（80pt以上）統一。
- **🔇 iOS マナーモード（サイレントスイッチ）連動消音対応 (F-20)**:
  - `TtsService` および `SoundService` の iOS オーディオセッションを `ambient` に設定。
  - マナーモード時のスピーカー自動消音と、イヤホン装着時の正常再生（iOS標準準拠）。
- **📊 リザルト画面の回答履歴バッジの省スペース・見切れ防止 (F-07)**:
  - 複数回回答時も `[2/3正解 ✓ ✗ ✓]` のようにスマートに集約したスリムバッジを採用。
- **⏸️ ゲーム開始カウントダウン中の一時停止対応 ＆ アプリ非アクティブ時自動ポーズ (F-02 / F-16)**:
  - カウントダウン中の一時停止ボタン常時表示、バックグラウンド移行時の自動ポーズ。
- **⚡ バーチャルスクロール完全最適化 ＆ 85px ゆとり枠 × 極限引き締め行間カード (F-06)**:
  - 34,650件の膨大リスト描画において、`SliverFixedExtentList(itemExtent: 85.0)` による厳格な固定高さウィンドウイングを適用。
  - 上部パディング（3.0px）、行間（0.0px ＋ 2行目 `Transform.translate(y: -3.0)` によるフォント余白物理相殺）、行高（1.2）を最適化し、スッキリとした上端寄りかつ行間がギュッと詰まった理想のレイアウトと、枠内見切れゼロを両立。
  - オフセット計算が O(1)（計算負荷ゼロ）となり、高速スクロールバーを激しくスライドさせても 60fps/120fps の超爆速・フリーズゼロ描画を実現。
- **⚡ カレンダー・スタンプ図鑑・キャラクター図鑑の起動・描画超高速化 (F-11 / F-12)**:
  - 画面遷移時の DB クエリ（月間レコード、ストリーク計算、全スタンプ、進捗データ）を `Future.wait` で並列フェッチ化し、データロード時間を最大 1/4 に短縮。
  - キャラクター図鑑のチャプター進捗参照を `Map<int, ChapterProgressesData>` で O(1) 高速ルックアップ化し、親画面からのキャッシュデータ引継ぎにより、画面を開いた瞬間にローディングなし（0ms）で即時描画。
- **🚀 CI/CD ワークフロー最適化 & 重複デプロイ完全排除 (F-18)**:
  - 重複していた古い Web デプロイワークフロー（`deploy_web.yml`）を完全削除し、`deploy.yml`（GitHub Pages）と `build_ios.yml`（IPAビルド）の2本に一本化。多重実行を防止。
- **🎴 キーボード収納時のリップル演出抑止 (F-06)**:
- **🔍 検索バーのUI洗練 & キーボード収納優先制御 (F-06)**:
  - 検索入力欄の文字およびプレースホルダーを「上下中央・左揃え（`TextAlignVertical.center` / `isDense: true`）」に美しく配置。
  - 未使用の音声トグルボタンを撤去し、検索入力欄を大幅に横幅拡大。右側の絞り込みボタン・三点メニューをスリム化して右寄せ。
  - 検索キーボード表示中に画面や単語カードをタップした際、誤って詳細モーダルを開かずにキーボードを確実に閉じる（`unfocus`）安全制御を実装。スクロール連動収納（`keyboardDismissBehavior: onDrag`）も対応。
- **⚡ レベル選択時のチャプター再描画（チラつき）完全解消 (F-03 / F-15)**:
  - ゲーム選択画面で難易度レベル（初級/中級/上級）を切り替えた際、メモリ上の進捗キャッシュから 0ms で即座に表示を切り替え。不要なローディングスピナー表示による画面のガクつき・チラつきを 100% 撤廃。
- **🗄️ 3NF正規化データベース移行 (F-01)**:
  - `WordSenses`、`UserWordProgresses` テーブルを導入し、DB整合性を強化。
- **🧹 不要アセットおよびデッドコードの削除 (F-18)**:
  - 4.85MB の中間CSV（`words_cleaned.csv`）および未使用ファイルの削除によるリポジトリ軽量化。

## [VER-20260824-04] - 2026-08-24
### 追加・修正内容
- **リスト型フィルター（ドロップダウン）の動的連動・件数表示 (F-06)**:
  - `CEFR`、`Chap`、`A-Z`、`Category` の各ドロップダウンの選択肢に、現在の他の絞り込み条件（未暗記・お気に入り・検索クエリ等）を反映した候補および該当件数（例: `Ch. 1 (12)` / `A1 (45)`）を動的表示。
- **和訳ボタンのUI統一**:
  - 和訳表示ボタンを他のフィルターボタンと完全に同一の高さ（28px）・角丸・トグルスタイル（`和訳: ON` / `和訳: OFF`）に揃え、統一感を向上。
- **単語カードのオーバーフロー（RIGHT OVERFLOWED BY 44 PIXELS）解消 (`lib/widgets/word_card_tile.dart`)**:
  - タイトル行を `Wrap` 構造に変更し、画面幅が狭い端末でも制限バッジ（`⚠️ 本日上限70pt`）や発音記号が適切に折り返されオーバーフローしないよう修正。

## [VER-20260824-03] - 2026-08-24
### 追加・修正内容
- **単語帳フィルターバーの機能強化 (F-06)**:
  - 「すべて」チップを廃止し、未選択時は自動的に全件を対象とするトグル方式に変更。
  - 「未暗記」と「お気に入り ★」の同時選択（AND条件）に対応。
  - `CEFR: 全て ▾`、`Chap: 全て ▾`、`A-Z: 全て ▾`、`Category: 全て ▾` の4大ドロップダウンフィルターを実装（データから動的抽出）。
  - スマホ等の画面幅が狭い端末に対応するため、フィルターバーを横スクロールスライド（`SingleChildScrollView(scrollDirection: Axis.horizontal)`）可能に改修。
- **仕様書・要件定義書の更新**:
  - `docs/REQUIREMENTS.md` の機能ステータス（F-03, F-06, F-15 を「動作確認済」）および F-06 詳細仕様を更新。
  - `docs/design/画面イメージ/【仕様書】単語帳.md` のフィルター仕様を更新。

## [VER-20260824-02] - 2026-08-24
### 追加・修正内容
- **単語帳画面のUI刷新 (F-06)**:
  - パステル調デザイン（背景 `#FBF7EE`、カード `#FFFDF9`、ボーダー `#E5DEC9`、プライマリ `#5F9E98`、セカンダリ `#ECA882`）への全面リニューアル。
  - 単語カード（角丸16px、微細シャドウ、発音記号、和訳一時表示、制限タグ `⚠️ 本日上限70pt`）のデザイン統一。
- **3種ソートモード（A-Z / Chap / Category）の実装**:
  - `A-Z`: 頭文字アルファベット別セクション化。
  - `Chap`: 通しチャプター別セクション化＆暗記率プログレスバー表示。
  - `Category`: 単語テーマ別セクション化＆進捗バー表示。
- **可変ヘッダー（Scroll-aware Floating Header）の実装**:
  - 下スクロール時に検索バー・フィルタータグ・ソートバーが画面外へスライドアウトし、上スクロールでなめらかに再表示される `SliverAppBar(floating: true)` 構造を採用。
- **粘着セクションヘッダー（Sticky Section Header）の新設 (`lib/widgets/sticky_section_header.dart`)**:
  - スクロール時も現在のセクション見出しと暗記率が画面最上部に固定される構造。
- **フィルタータグの拡充**:
  - 「すべて」「未暗記 (Unlearned)」「お気に入り ★」「CEFRレベル (A1〜C2)」によるワンタップ絞り込み。

## [VER-20260824-01] - 2026-08-24
### 追加・修正内容
- **下部ナビゲーションの3タブ化 (F-15)**:
  - 「ゲーム」「単語一覧」「記録」の3タブ構成に再編。
  - パステル調テーマカラー（背景 `#FBF7EE`、プライマリ `#5F9E98`、セカンダリ `#ECA882`）の適用。
- **「記録」タブ画面の新設 (`lib/screens/records_screen.dart`)**:
  - カレンダー、スタンプ図鑑、キャラクター図鑑への導線メニューカードを配置。
  - 連続プレイ日数（ストリーク）のサマリー表示。
- **チャプター進行状況＆解放制御の永続化 (`ChapterProgresses` テーブル)**:
  - 初回起動時は各レベルの最初のチャプター（Ch.1等）のみ解放。
  - 学習モードクリア時に暗記率 **90%超**（>90%）達成で次チャプターを自動解放・永続化。
  - チャレンジモードではチャプター解放を行わない仕様を徹底。
- **ゲーム選択画面の改修 (`lib/screens/mode_select_screen.dart`)**:
  - レベル（初級/中級/上級）選択時、解放済みの最新チャプターを自動で初期選択。
  - 未解放チャプターのロック（鍵アイコン表示・選択不可）。
  - クリア済みチャプターの達成率（%）と王冠バッジ表示。
- **リザルト画面のチャプター解放演出 (`lib/screens/game_screen.dart`)**:
  - 学習モードクリア時にチャプター暗記率と次章解放メッセージを表示。

## [VER-20260817-121] - 2026-08-17
### 変更・更新内容
- **要件定義書の同期**: 2レーン時差落下ゲーム性および起動画面仕様の最新状態を反映。

## [VER-20260817-120] - 2026-08-17
### 追加・修正内容
- **2レーン時差落下処理の実装**:
  - ゲーム開始時、右レーンが先行して落下を開始。
  - 右レーンスタートから2秒後に左レーンが自動追随スタート。
  - 右レーン解答完了時、左レーンが未スタートであれば2秒を待たずに即時スタート。
- **起動画面（難易度選択）の復元**:
  - 初級（LV1）・中級（LV2）・上級（LV3）を選択してゲームを開始するUI構造を再構築。
- **リザルト・復習機能の強化**:
  - 60秒終了後、要復習単語リストを表示（TTS音声再生・お気に入りトグル機能付き）。